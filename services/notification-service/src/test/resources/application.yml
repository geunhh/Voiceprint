session:
  key: "dummy-session-key"
message:
  key: "dummy-message-key"
spring:
  cloud:
    config:
      enabled: false
  data:
    redis:
      host: localhost
      port: 6379
      password: ""
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
