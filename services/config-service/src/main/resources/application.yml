server:
  port: 8888

spring:
  application:
    name: config-service
  profiles:
    active : native
  cloud:
    config:
      server:
#        git:
#          uri: file:///Users/geunhwi/Desktop/Voiceprint/config-repo # 로컬의 config-repo 디렉토리 사용
        native:
          search-locations: file:///Users/geunhwi/Desktop/Voiceprint/config-repo # 로컬의 config-repo 디렉토리 사용

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/